# ============================================================
# Конфигурация Telegram-бота-сканера криптовалют
# ============================================================

# --- Telegram ---
# Токен бота (получить у @BotFather)
BOT_TOKEN = ''

# Chat ID для отправки уведомлений (получить у @userinfobot)
CHAT_ID = ''

# --- Выбор биржи ---
# Доступные значения: 'binance', 'bybit'
# Определяет, с какой биржей работает бот через ccxt
ACTIVE_EXCHANGE = 'binance'

# --- Мульти-сканирование ---
# Список бирж для одновременного сканирования
# Пример: ['binance', 'bybit'] — сканировать обе биржи параллельно
# Пример: ['binance'] — только Binance (одиночный режим)
EXCHANGES_TO_SCAN = ['binance', 'bybit']

# --- Binance (через ccxt) ---
# Настройки подключения к бирже Binance
BINANCE_API_KEY = ''          # API-ключ Binance (необязательно для публичных данных)
BINANCE_SECRET_KEY = ''       # Секретный ключ Binance (необязательно для публичных данных)
BINANCE_SANDBOX = False       # Использовать тестовую среду (True/False)

# --- Bybit (через ccxt) ---
# Настройки подключения к бирже Bybit
BYBIT_API_KEY = ''            # API-ключ Bybit (необязательно для публичных данных)
BYBIT_SECRET_KEY = ''         # Секретный ключ Bybit (необязательно для публичных данных)
BYBIT_SANDBOX = False         # Использовать тестовую среду (True/False)

# --- Пороги пампа ---
# Условие: цена выросла более чем на PUMP_PRICE_CHANGE_PERCENT за PUMP_TIMEFRAME минут,
# при этом текущий объём превышает средний в PUMP_VOLUME_MULTIPLIER раз
PUMP_PRICE_CHANGE_PERCENT = 8       # Минимальный рост цены в процентах
PUMP_TIMEFRAME_MINUTES = 5          # Период наблюдения (минуты)
PUMP_VOLUME_MULTIPLIER = 5          # Превышение объёма над средним (множитель)

# --- Пороги дампа ---
# Условие: цена упала более чем на DUMP_PRICE_CHANGE_PERCENT за DUMP_TIMEFRAME минут,
# при этом текущий объём превышает средний в DUMP_VOLUME_MULTIPLIER раз
DUMP_PRICE_CHANGE_PERCENT = 5       # Минимальное падение цены в процентах
DUMP_TIMEFRAME_MINUTES = 5          # Период наблюдения (минуты)
DUMP_VOLUME_MULTIPLIER = 4          # Превышение объёма над средним (множитель)

# --- Кулдаун ---
# Время (в секундах), в течение которого сигнал по монете не отправляется повторно
COOLDOWN_SECONDS = 1800             # 30 минут = 30 * 60 = 1800 секунд

# --- Параметры сканирования ---
# Количество монет из топа по объёму для проверки
TOP_COINS_COUNT = 50                # Проверяем топ-50 монет по 24h объёму

# Интервал между циклами сканирования (в секундах)
SCAN_INTERVAL_SECONDS = 60          # Сканирование каждые 60 секунд

# --- Стоп-лист монет ---
# Эти пары исключены из сканирования (стейблкоины и дубли)
STOP_COINS = [
    'USDC/USDT',
    'BUSD/USDT',
    'FDUSD/USDT',
]